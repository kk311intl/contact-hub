{
  "name": "contact-hub",
  "version": "1.1.0",
  "private": true,
  "license": "GPL-3.0-only",
  "type": "module",
  "engines": { "node": ">=22.0.0" },
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "node --test && wrangler deploy --dry-run"
  },
  "dependencies": {},
  "devDependencies": {
    "wrangler": "^4.42.0"
  }
}
